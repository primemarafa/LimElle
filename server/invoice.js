export function renderInvoice(order) {
  const esc = (value) => String(value ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#039;");
  const money = (value) => new Intl.NumberFormat("fr-FR").format(value) + " FCFA";
  const rows = order.items.map((item) => `<tr><td>${esc(item.product.name)}</td><td>${item.quantity}</td><td>${money(item.product.price)}</td><td>${money(item.product.price * item.quantity)}</td></tr>`).join("");
  return `<!doctype html><html lang="fr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Facture ${esc(order.reference)} | Lim'Elle</title><style>body{font-family:Arial,sans-serif;margin:40px;color:#2f241f;background:#fffaf4}.invoice{max-width:800px;margin:auto;background:#fff;padding:40px;border:1px solid #eadfd5;border-radius:12px}h1{margin:0;color:#5a3025}.muted{color:#75665e}.head{display:flex;justify-content:space-between;gap:24px;margin-bottom:36px}table{width:100%;border-collapse:collapse;margin:28px 0}th,td{padding:12px;border-bottom:1px solid #eadfd5;text-align:left}.total{margin-left:auto;max-width:320px}.line{display:flex;justify-content:space-between;padding:7px 0}.grand{font-size:20px;font-weight:700;border-top:2px solid #5a3025;margin-top:8px;padding-top:12px}button{padding:10px 18px;border:0;background:#B58A4A;color:white;font-weight:bold;border-radius:8px;cursor:pointer;margin-top:16px}@media print{body{margin:0;background:white}.invoice{border:0;padding:0}button{display:none}}</style></head><body><main class="invoice"><div class="head"><div><h1>Lim'Elle</h1><p class="muted">Facture client</p></div><div><strong>Référence</strong><br>${esc(order.reference)}<br><span class="muted">${esc(order.createdAt)}</span></div></div><section><strong>Client</strong><p>${esc(order.customer.fullName)}<br>${esc(order.customer.phone)}<br>${esc(order.customer.city)}</p></section><section><strong>Livraison</strong><p>${esc(order.deliveryMode === "domicile" ? "À domicile" : "Point de retrait")}${order.deliveryAddress ? `<br>${esc(order.deliveryAddress)}` : ""}</p></section><table><thead><tr><th>Produit</th><th>Qté</th><th>Prix unitaire</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table><div class="total"><div class="line"><span>Produits</span><span>${money(order.totals.productTotal)}</span></div><div class="line"><span>Transport</span><span>${money(order.totals.transport)}</span></div><div class="line grand"><span>Total</span><span>${money(order.totals.total)}</span></div></div><p class="muted">Statut : ${esc(order.status)}</p><button id="printBtn" onclick="window.print()">Imprimer / enregistrer en PDF</button><script>document.getElementById("printBtn")?.addEventListener("click",function(){window.print()});</script></main></body></html>`;
}
