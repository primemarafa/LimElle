Phase 4 backend fix summary

Le backend valide maintenant les commandes, recalcule les totaux, génère les références, conserve les commandes en mémoire et expose leur récupération par référence. Les tests backend sont exécutés par CI avant le build frontend.