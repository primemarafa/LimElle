// Phase 1 entry point.
// The current prototype remains at the repository root while the migration
// is performed incrementally. New application code should live under src/.
import LimElleSite from "../LimElleSite";

export default function App() {
  return <LimElleSite />;
}
